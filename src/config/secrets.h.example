// Template. Copy to secrets.h, or let tools/get_refresh_token.py write it.
//
// secrets.h is gitignored and holds durable access to your Spotify account.
// Never commit it, never paste its contents into a chat or an issue.
#pragma once

#define WIFI_SSID     ""
#define WIFI_PASSWORD ""

#define SPOTIFY_CLIENT_ID     ""
#define SPOTIFY_CLIENT_SECRET ""
#define SPOTIFY_REFRESH_TOKEN ""

// Shared secret for the Mac link helper (tools/mac_link.py). Leave empty to
// disable the command channel entirely; sleep reporting still works.
#define MAC_LINK_TOKEN ""
